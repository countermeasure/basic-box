# The "lynis audit system" command appears to only inspect the system, and
# doesn't appear to modify it in any way, so there doesn't seem to be a
# significant security risk associated with making it passwordless.
%sudo ALL=(root) NOPASSWD: /usr/bin/journalctl
