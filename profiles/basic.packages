# On Debian 12, simple-cdd 0.6.8 fails to include zstd, without which the
# machine will fail to boot.
zstd

firmware-iwlwifi
firmware-linux-free
firmware-misc-nonfree
# The intel-microcode package isn't needed to do the installation, but it is
# recommended. See: https://wiki.debian.org/Microcode
intel-microcode
